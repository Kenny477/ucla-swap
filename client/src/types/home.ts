export interface mainHero {
    title: string;
    subtitle: string;
    description: string;
    img: string;
    primaryAction: {
      text: string;
      href: string;
    },
}

export interface heroImage {
    
}